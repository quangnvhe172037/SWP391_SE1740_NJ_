import { useParams } from "react-router-dom";

const EditLessonArticle = (prop) => {
  const { subjectId } = useParams();
   const lessonId = prop.lessonId;
    return <div className="">
      
  </div>;
};

export default EditLessonArticle;
